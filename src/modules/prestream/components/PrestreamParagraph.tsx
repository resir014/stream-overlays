import styled from '@emotion/styled'

const PrestreamParagraph = styled('p')`
  margin: 0 0 8px;
  font-size: 28px;
  line-height: 1.5;

  &:last-of-type {
    margin-bottom: 0;
  }

  strong {
    display: inline;
    line-height: 1;
  }
`

export default PrestreamParagraph
